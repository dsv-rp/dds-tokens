node_modules
build

previous-release
.changeset/auto-generated-token-changes.md

# `crate-ci/typos` action downloads `typos` binary to the repo root.
# Prevent being included in release PR.
typos

.DS_Store
